#ifndef _SJF_
#define _SJF_
#include "../process.c"
#include "../sort.c"
#include "../vector.c"
#include <stdio.h>

void SJF(Process *p, Vector *v, int len){
    int trt, twt, tat, tct = 0;

    process_init(p, len);
}
#endif